import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    justifyContent: "center",
    alignItems: "center",
    paddingTop: 48,
  },
  image: {
    marginBottom: 16,
  },
  text: {
    color: "#808080",
    fontSize: 14,
    textAlign: "center",
  },
  bold: {
    fontWeight: "700",
  },
});
